{
  "name": "useapiguard",
  "version": "1.0.1",
  "description": "A reusable React hook (like `useIsValidRequest`) that:",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsup src/index.ts --format esm, cjs --dts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^19.2.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/react": "^19.2.6",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
